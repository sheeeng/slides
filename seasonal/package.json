{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/prepare-preview.mjs && vite build",
    "dev": "node scripts/prepare-preview.mjs && vite",
    "test": "node --test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "6.1.1",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "vite": "8.2.2"
  }
}
